# Use a default tmux so you don't have to start it manually
# This should probably be in something like a .zshrc_ssh
# or something
if command -v tmux &> /dev/null && [ -z "$TMUX" ];
  tmux attach -t default || tmux new -s default
fi
