# Use a default tmux so you don't have to start it manually
if command -v tmux &> /dev/null && [ -z "$TMUX" ]; then
  tmux attach -t default || tmux new -s default
fi

# Since this is connected remotely, display the hostname
export PS1='🚀@%m %1~ > '
